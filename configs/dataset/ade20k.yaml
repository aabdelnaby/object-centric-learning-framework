# ADE20K dataset in WebDataset format (placeholders).
_target_: ocl.datasets.WebdatasetDataModule

# Replace the shard globs and sizes with your local paths/counts.
train_shards: "${oc.env:DATASET_PREFIX}/ade20k/train/shard-{000000..000099}.tar"
train_size: null
val_shards: "${oc.env:DATASET_PREFIX}/ade20k/val/shard-{000000..000009}.tar"
val_size: null
test_shards: "${oc.env:DATASET_PREFIX}/ade20k/val/shard-{000000..000009}.tar"
test_size: null

use_autopadding: true
num_workers: 4
batch_size: 8
